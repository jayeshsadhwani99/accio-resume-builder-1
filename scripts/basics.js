// Data Types
String;
Number;
Boolean;
null;
undefined;
Array; // -> list elements arr = [1, 2, 3]
Object; // -> collection of key, value pairs

// arr[1] -> 2

// All arrays have indexes, and indexes start from 0

// Define variables - var, let, const

// Opeartors
// +, -, * -> arithmetic
// >, <, ==, <=, >=, !=, === -> comparison
// = -> assignment operator
// && -> AND, || -> OR, ! -> NOT
// ++, -- -> increment/decrement operator (+1) (-1)

// Control Statements

// IF-ELSE
let age = 20;

if (age > 18) {
  console.log("You can vote");
} else {
  console.log("You cannot");
}

// LOOPS
for (let i = 0; i < 11; i++) {
  console.log(i);
}
